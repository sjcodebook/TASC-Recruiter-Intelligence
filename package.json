{
  "name": "tasc-match-intelligence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n api,web -c cyan,green \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix server run build && npm --prefix web run build",
    "test": "npm --prefix server test",
    "db:up": "docker compose up -d pgvector",
    "db:down": "docker compose down",
    "db:setup": "npm --prefix server run db:migrate && npm --prefix server run db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

